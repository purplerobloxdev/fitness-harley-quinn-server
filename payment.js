// Change the fetch URL to your deployed server URL
const response = await fetch('https://fitness-harley-quinn-server.onrender.com', {
    method: 'POST',
    headers: {
        'Content-Type': 'application/json'
    },
    // ... rest of the code
}); 